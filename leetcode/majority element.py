
nums = [3,2,3]
size=len(nums)
majority = size/2
majority=round(majority)

emp=[]
for outerloop in range(0,len(nums)):
    serach_number=nums[outerloop]
    for allnumb in nums:
        if serach_number == allnumb:
            emp.append(serach_number)
    for majority in :
print(emp)
